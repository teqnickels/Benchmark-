import chai, {expect} from 'chai'
import chaiChange from 'chai-change'
import guessTheNumber from '../lib/guessTheNumber'

describe('guessTheNumber', () => {
  it('if the user guess matches the generated number' () => {
      expect(guessTheNumber().toEqual())

  })
})
